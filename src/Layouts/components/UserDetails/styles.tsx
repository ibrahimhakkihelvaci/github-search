import { makeStyles, fade } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
	container: {
		width: "100%",
		height: "100%",
		paddingLeft: 77,
		paddingTop: 40,
	},
	content: {
		marginTop: 30,
	},
}));

export { useStyles };
